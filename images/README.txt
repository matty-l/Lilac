# In this folder goes textures, bump maps, and saved images if redirected. I've also thrown some of the images used in the
# documentation into this folder, though that usage should not be followed as a model
